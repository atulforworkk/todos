type Props = {};

const LoginHeaderButton = (props: Props) => {
  return (
    <div>
      <button className=" px-6 py-1 mr-10 font-semibold border-2 rounded-full text-md border-orange-color bg-orange-color hover:text-orange-color hover:bg-transparent">
        14 days free trail
      </button>
    </div>
  );
};

export default LoginHeaderButton;
